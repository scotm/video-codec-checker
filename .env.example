# Example environment variables for video-codec-checker

# Output CSV filename (if omitted, a timestamped filename is generated)
# OUTPUT_FILE=video_codec_check_results.csv

# Directory to scan (default: current directory)
# SCAN_DIRECTORY=/path/to/videos

# Cleanup behavior in generated scripts
# DELETE_ORIGINAL=0   # set to 1/true/yes/on to delete source after successful conversion
# TRASH_ORIGINAL=0    # set to 1/true/yes/on to move source to Trash after successful conversion

# Fast ffprobe controls (fast-probe enabled by default)
# FAST_PROBE=1
# FFPROBE_PROBESIZE=5M
# FFPROBE_ANALYZEDURATION=10M
